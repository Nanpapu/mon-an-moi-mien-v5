// Thời gian tối đa cho phép load (30 giây)
export const MAP_LOAD_TIMEOUT = 30000;

// Thời gian retry giữa các lần load (3 giây)
export const RETRY_DELAY = 3000;

// Số lần retry tối đa
export const MAX_RETRIES = 3;
