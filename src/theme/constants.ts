// Các hằng số được sử dụng trong theme
export const THEME_CONSTANTS = {
  animation: {
    duration: 150,    // Thời gian chuyển động mặc định (ms)
    scale: 1.2,       // Tỉ lệ scale khi tương tác
  },
  layout: {
    tabBarHeight: 60, // Chiều cao của thanh tab
    tabBarPadding: 5, // Padding của thanh tab
    borderRadius: {
      sm: 8,          // Border radius nhỏ
      md: 12,         // Border radius vừa
      lg: 16,         // Border radius lớn
    }
  }
}; 